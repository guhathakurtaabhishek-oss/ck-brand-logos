Brand logo PNGs (optional)
==========================
Drop transparent-background brand logo PNGs here, named by brand (lowercase, 
spaces→underscore, &→and). Examples:
  dot_and_key.png
  mamaearth.png
  myntra.png
  ajio.png

If a matching PNG exists, generate_proof_frame() composites it onto the card.
If not, the brand name is rendered as bold text (always works, brand is a variable).
